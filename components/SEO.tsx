/**
 * SEO Component
 * This component is no longer needed in Next.js 13+ App Router
 * as metadata is now handled via generateMetadata() in each page
 * 
 * Kept for reference and backwards compatibility
 */

export function SEO() {
  return null
}

